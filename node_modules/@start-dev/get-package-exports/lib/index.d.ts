export interface NormalizedExport {
    exportName: string;
    resolvedPath: string;
}
interface PackageExportOptions {
    allowedExportKeys: string[];
    overrideExports?: {
        [key: string]: string;
    };
}
export default function getPackageExports(packageDirectory: string, options: PackageExportOptions): Promise<NormalizedExport[]>;
export {};
