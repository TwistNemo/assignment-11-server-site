export interface StartDevOptions {
    appDirectory: string;
    appEntrypoint?: string;
    portNumber: number;
    cacheDirectory?: string;
    packageExportsOverrides?: {
        [key: string]: undefined | {
            [key: string]: string;
        };
    };
}
export default function createDevServer({ appDirectory, appEntrypoint, cacheDirectory: $cacheDirectory, portNumber, packageExportsOverrides, }: StartDevOptions): void;
