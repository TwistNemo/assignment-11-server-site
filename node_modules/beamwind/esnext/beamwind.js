// src/index.ts
import tailwind from "@beamwind/preset-tailwind";
import preflight2 from "@beamwind/preflight";
import {createInstance} from "@beamwind/core";
import {
  strict,
  warn,
  noprefix,
  cyrb32,
  cssomInjector,
  noOpInjector,
  virtualInjector,
  createInstance as createInstance2
} from "@beamwind/core";
var instance = createInstance([tailwind(), preflight2()]);
var {bw} = instance;
var {setup} = instance;
var {theme} = instance;
export {
  bw,
  createInstance2 as createInstance,
  cssomInjector,
  cyrb32,
  noOpInjector,
  noprefix,
  setup,
  strict,
  theme,
  virtualInjector,
  warn
};
